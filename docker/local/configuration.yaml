app_host: 0.0.0.0
app_port: 8000
auth_url: https://dev.try.direct/server/user/oauth_server/api/me
max_clients_number: 2

database:
  host: stackerdb
  port: 5432
  username: postgres
  password: postgres
  database_name: stacker

amqp:
  host: 127.0.0.1
  port: 5672

# Vault configuration — override address/token via VAULT_ADDRESS / VAULT_TOKEN env vars
vault:
  address: http://vault2.try.direct:8200
  token: change-me-in-env
  api_prefix: v1
  agent_path_prefix: agent
  username: guest
  password: guest
